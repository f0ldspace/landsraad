{ config, pkgs, lib, ... }:

{
  # GNOME user configuration
  # Add dconf settings, extensions, etc. here as needed

  # Example: Enable dark theme
  # dconf.settings = {
  #   "org/gnome/desktop/interface" = {
  #     color-scheme = "prefer-dark";
  #   };
  # };
}
